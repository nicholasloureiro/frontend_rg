# Base URL of the roupadegala Django backend.
# In development, point to your local backend (e.g. http://localhost:8000).
# In production, this is set at build time to the Railway backend.
VITE_API_BASE_URL=http://localhost:8000

# Optional: set to "true" to enable verbose logging in services/api.js
# VITE_DEBUG=false
